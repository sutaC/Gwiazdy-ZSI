<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../components/globalHead.ejs') %>
        <title>Gwiazdy ZSI | Login</title>
        <link rel="stylesheet" href="/style/forms.css" />
    </head>

    <body>
        <main>
            <h1>Login</h1>

            <form
                action="/login"
                method="post"
                hx-boost="true"
                hx-target="#error"
            >
                <small id="error"></small>

                <div class="field">
                    <label for="login">Login</label>
                    <input
                        type="text"
                        id="login"
                        name="login"
                        placeholder="Login"
                        required
                    />
                </div>

                <div class="field">
                    <label for="password">Password</label>
                    <input
                        type="password"
                        id="password"
                        name="password"
                        placeholder="Password"
                        required
                    />
                </div>

                <button type="submit">Login</button>
            </form>

            <a href="/">Go back</a>
        </main>
    </body>
</html>
